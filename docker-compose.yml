services:
  workbench:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./memory:/app/memory
      - ./app.py:/app/app.py
      - ./core:/app/core
      - ./static:/app/static
    extra_hosts:
      - "host.docker.internal:host-gateway"
