[project]
name = "sih-2026"
version = "0.1.0"
description = "Document analysis pipeline for PDFs, Word files, spreadsheets, CSVs, and images"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.2",
    "sqlite-vec>=0.1.9",
    "streamlit>=1.62.0",
]
